# MoReMouse Pipeline Overview

> 단안 이미지에서 실험용 생쥐의 3D 재구성을 위한 전체 파이프라인 설명

---

## 목차
1. [전체 아키텍처](#1-전체-아키텍처)
2. [Stage 1: Gaussian Mouse Avatar (AGAM)](#2-stage-1-gaussian-mouse-avatar-agam)
3. [Stage 2: 합성 데이터 생성](#3-stage-2-합성-데이터-생성)
4. [Stage 3: MoReMouse 네트워크](#4-stage-3-moremouse-네트워크)
5. [Stage 4: DMTet 메쉬 추출](#5-stage-4-dmtet-메쉬-추출)
6. [학습 설정 (Paper)](#6-학습-설정-paper)

---

## 1. 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          MoReMouse Pipeline                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌───────────┐  │
│  │  Multi-view  │───▶│   Gaussian   │───▶│   Synthetic  │───▶│ MoReMouse │  │
│  │    Videos    │    │    Avatar    │    │     Data     │    │  Network  │  │
│  │   (8 cams)   │    │   (AGAM)     │    │  (12K scenes)│    │           │  │
│  └──────────────┘    └──────────────┘    └──────────────┘    └─────┬─────┘  │
│                                                                     │        │
│                                                                     ▼        │
│                           ┌──────────────┐    ┌──────────────────────────┐  │
│                           │   Inference  │◀───│  NeRF → DMTet → Mesh     │  │
│                           │  (단안 이미지)│    │  (Density → SDF → Mesh)  │  │
│                           └──────────────┘    └──────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 핵심 아이디어
1. **문제**: 단안(monocular) 이미지에서 3D 재구성은 ill-posed problem
2. **해결책**:
   - Multi-view 데이터로 photorealistic avatar 학습
   - Avatar로 대규모 합성 데이터 생성 (다양한 pose, viewpoint)
   - 합성 데이터로 단안→3D 네트워크 학습

---

## 2. Stage 1: Gaussian Mouse Avatar (AGAM)

### 2.1 목적
Multi-view 비디오에서 **포토리얼리스틱한 생쥐 아바타**를 학습하여, 이후 합성 데이터 생성에 사용

### 2.2 입력 데이터
```
/home/joon/data/markerless_mouse_1_nerf/
├── videos_undist/
│   ├── 0.mp4      # Camera 0 (동기화된 multi-view)
│   ├── 1.mp4      # Camera 1
│   ├── ...
│   └── 5.mp4      # Camera 5 (총 6대)
└── calibration/   # 카메라 캘리브레이션
```

### 2.3 MAMMAL Body Model
```python
# 140개 관절을 가진 Articulated Mouse Model
body_model = load_mouse_model("mouse_model/mouse.pkl")
# - v_template: [13059, 3] 기본 메쉬 정점
# - faces: [25350, 3] 삼각형 면
# - skinning_weights: [13059, 140] LBS 가중치
# - joint_regressor: 관절 위치 계산
```

### 2.4 Gaussian Splatting on UV Space

```
┌─────────────────────────────────────────────────────────────────┐
│                    Gaussian Avatar Architecture                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   UV Space (Learnable)              3D Space (Rendered)         │
│   ┌─────────────────┐               ┌─────────────────┐         │
│   │  ○ ○ ○ ○ ○ ○   │   Deform      │                 │         │
│   │  ○ ○ ○ ○ ○ ○   │  ────────▶    │    ┌─────┐      │         │
│   │  ○ ○ ○ ○ ○ ○   │   (LBS)       │   /  ◎    \     │         │
│   │  ○ ○ ○ ○ ○ ○   │               │  │   ◎◎    │    │         │
│   └─────────────────┘               │   \______/     │         │
│   Gaussians per vertex              │  3D Gaussians  │         │
│                                     └─────────────────┘         │
│                                                                  │
│   Parameters per Gaussian:                                       │
│   - position offset (Δx, Δy, Δz): 메쉬 정점 기준 오프셋         │
│   - scale (sx, sy, sz): Gaussian 크기                           │
│   - rotation (quaternion): Gaussian 방향                        │
│   - opacity (α): 불투명도                                       │
│   - color (SH coefficients): 뷰 의존적 색상                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.5 Linear Blend Skinning (LBS)

포즈에 따라 Gaussian 위치를 변형:

```python
def forward_lbs(vertices, pose, skinning_weights, joint_transforms):
    """
    vertices: [V, 3] - 정점 위치
    pose: [J, 3] - 관절 회전 (axis-angle)
    skinning_weights: [V, J] - 각 정점이 각 관절에 영향받는 정도

    Returns: [V, 3] - 변형된 정점 위치
    """
    # 1. Pose → Joint Transforms (4x4 matrices)
    # 2. Blend transforms: T_blended = Σ w_j * T_j
    # 3. Apply: v' = T_blended @ v
```

### 2.6 렌더링 (gsplat)

```python
# Differentiable Gaussian Splatting
rendered_image = rasterization(
    means=gaussian_positions,      # [N, 3] 3D 위치
    quats=gaussian_rotations,      # [N, 4] 회전
    scales=gaussian_scales,        # [N, 3] 스케일
    opacities=gaussian_opacities,  # [N, 1] 불투명도
    colors=gaussian_colors,        # [N, 3] or [N, SH_dim] 색상
    viewmats=camera_extrinsics,    # [4, 4] 카메라 외부 파라미터
    Ks=camera_intrinsics,          # [3, 3] 카메라 내부 파라미터
)
```

### 2.7 학습 손실 함수

```python
L_total = L_rgb + λ_ssim * L_ssim + λ_lpips * L_lpips

# L_rgb: L1 픽셀 손실
L_rgb = |I_rendered - I_gt|

# L_ssim: 구조적 유사도 (패치 기반)
L_ssim = 1 - SSIM(I_rendered, I_gt)

# L_lpips: 지각적 손실 (VGG features)
L_lpips = ||VGG(I_rendered) - VGG(I_gt)||
```

---

## 3. Stage 2: 합성 데이터 생성

### 3.1 목적
학습된 Avatar를 사용하여 **대규모 (pose, viewpoint) 쌍** 생성

### 3.2 데이터 생성 과정

```
┌─────────────────────────────────────────────────────────────────┐
│                    Synthetic Data Generation                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  For each scene (12,000 total):                                 │
│                                                                  │
│  1. Sample Random Pose                                          │
│     ┌──────────────────┐                                        │
│     │ pose ~ P(poses)  │  ← MAMMAL fitting 결과에서 샘플링      │
│     └────────┬─────────┘    또는 랜덤 augmentation              │
│              │                                                   │
│              ▼                                                   │
│  2. Deform Avatar (LBS)                                         │
│     ┌──────────────────┐                                        │
│     │ Avatar(pose) → │  ← Gaussian positions 변형              │
│     │ Posed Gaussians │                                         │
│     └────────┬─────────┘                                        │
│              │                                                   │
│              ▼                                                   │
│  3. Render Multi-view (64 views per scene)                      │
│     ┌──────────────────────────────────────────┐                │
│     │  View 1   View 2   ...   View 64         │                │
│     │  ┌───┐    ┌───┐          ┌───┐           │                │
│     │  │ 🐭│    │ 🐭│          │ 🐭│           │                │
│     │  └───┘    └───┘          └───┘           │                │
│     │  θ=0°    θ=5.6°        θ=354.4°         │                │
│     └──────────────────────────────────────────┘                │
│                                                                  │
│  Output per scene:                                               │
│  - images: [64, H, W, 3]     렌더링된 이미지                    │
│  - cameras: [64, 4, 4]       카메라 파라미터                    │
│  - mesh: [V, 3]              GT 메쉬 (from body model)          │
│  - pose: [J*3]               MAMMAL pose                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 카메라 샘플링

```python
def sample_cameras(num_views=64, radius=2.22, elevation_range=(-30, 60)):
    """
    구 표면에서 균등하게 카메라 위치 샘플링

    - Azimuth: 0° ~ 360° (균등 분할)
    - Elevation: -30° ~ 60° (랜덤 샘플링)
    - 모든 카메라는 원점(생쥐 중심)을 바라봄
    """
```

---

## 4. Stage 3: MoReMouse 네트워크

### 4.1 목적
단안 이미지 → 3D NeRF representation 학습

### 4.2 네트워크 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    MoReMouse Network                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Input Image          Encoder              Triplane Features    │
│  ┌─────────┐         ┌─────────┐          ┌─────────────────┐   │
│  │         │         │         │          │  XY   XZ   YZ   │   │
│  │   🐭    │ ──────▶ │  DINOv2 │ ──────▶  │  ┌─┐  ┌─┐  ┌─┐  │   │
│  │         │         │ + Neck  │          │  └─┘  └─┘  └─┘  │   │
│  └─────────┘         └─────────┘          └────────┬────────┘   │
│  [B, 3, H, W]        [B, C, h, w]               [B, 3, C, h, w] │
│                                                     │           │
│                                                     ▼           │
│                      ┌──────────────────────────────────────┐   │
│                      │         NeRF Decoder                 │   │
│                      │  ┌─────────────────────────────────┐ │   │
│                      │  │ For each 3D point (x, y, z):    │ │   │
│                      │  │                                  │ │   │
│                      │  │ 1. Sample triplane features:     │ │   │
│                      │  │    f_xy = F_xy[x, y]            │ │   │
│                      │  │    f_xz = F_xz[x, z]            │ │   │
│                      │  │    f_yz = F_yz[y, z]            │ │   │
│                      │  │                                  │ │   │
│                      │  │ 2. Aggregate: f = f_xy+f_xz+f_yz│ │   │
│                      │  │                                  │ │   │
│                      │  │ 3. MLP → (density σ, color c)   │ │   │
│                      │  └─────────────────────────────────┘ │   │
│                      └──────────────────────────────────────┘   │
│                                          │                      │
│                                          ▼                      │
│                      ┌──────────────────────────────────────┐   │
│                      │       Volume Rendering               │   │
│                      │  I(r) = Σ T_i · α_i · c_i           │   │
│                      │  where T_i = Π(1 - α_j) for j < i   │   │
│                      └──────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 DINOv2 Encoder

```python
# Self-supervised Vision Transformer (pretrained)
class DINOv2Encoder(nn.Module):
    def __init__(self):
        self.dino = torch.hub.load('facebookresearch/dinov2', 'dinov2_vitb14')
        # Output: [B, 768, H/14, W/14] patch features

    def forward(self, x):
        features = self.dino.forward_features(x)
        return features  # Rich semantic features
```

### 4.4 Triplane Representation

```python
# 3D를 세 개의 2D 평면으로 표현 (메모리 효율적)
class TriplaneGenerator(nn.Module):
    def __init__(self, in_channels=768, plane_channels=32, plane_resolution=256):
        self.to_planes = nn.Conv2d(in_channels, plane_channels * 3, 1)

    def forward(self, features):
        # features: [B, C, h, w] from encoder
        planes = self.to_planes(features)  # [B, 3*C', h, w]
        planes = planes.reshape(B, 3, C', h, w)  # [B, 3, C', h, w]
        # planes[0]: XY plane, planes[1]: XZ plane, planes[2]: YZ plane
        return planes
```

### 4.5 NeRF Volume Rendering

```python
def volume_render(density, color, z_vals, rays_d):
    """
    density: [B, N_rays, N_samples] - 밀도 σ
    color: [B, N_rays, N_samples, 3] - RGB 색상
    z_vals: [B, N_rays, N_samples] - 샘플링 깊이

    Returns:
        rgb: [B, N_rays, 3] - 렌더링된 색상
        depth: [B, N_rays] - 예측 깊이
    """
    # 1. 간격 계산
    dists = z_vals[..., 1:] - z_vals[..., :-1]

    # 2. Alpha 계산: α = 1 - exp(-σ * Δt)
    alpha = 1 - torch.exp(-density * dists)

    # 3. Transmittance: T = Π(1 - α)
    T = torch.cumprod(1 - alpha + 1e-10, dim=-1)

    # 4. Weights: w = T * α
    weights = alpha * T

    # 5. RGB 합성: C = Σ w_i * c_i
    rgb = (weights[..., None] * color).sum(dim=-2)

    # 6. Depth: D = Σ w_i * t_i
    depth = (weights * z_vals).sum(dim=-1)

    return rgb, depth, weights
```

### 4.6 NeRF 학습 손실

```python
L_nerf = L_rgb + λ_depth * L_depth + λ_mask * L_mask

# RGB 손실 (novel view synthesis)
L_rgb = ||I_pred - I_gt||_1

# Depth 손실 (기하학적 정확도)
L_depth = ||D_pred - D_gt||_1

# Mask 손실 (실루엣 일치)
L_mask = BCE(M_pred, M_gt)
```

---

## 5. Stage 4: DMTet 메쉬 추출

### 5.1 목적
NeRF의 implicit density → explicit mesh 변환

### 5.2 DMTet (Deep Marching Tetrahedra)

```
┌─────────────────────────────────────────────────────────────────┐
│                    NeRF → DMTet → Mesh                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Step 1: Density → SDF 변환                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                          │    │
│  │  NeRF Density σ(x)        SDF s(x)                      │    │
│  │  ┌─────────────┐          ┌─────────────┐               │    │
│  │  │ ░░░████░░░  │  ──────▶ │ ─────0───── │               │    │
│  │  │ ░░████░░░░  │  MLP     │ ──0────────  │               │    │
│  │  │ ░░░███░░░░  │          │ ────0─────── │               │    │
│  │  └─────────────┘          └─────────────┘               │    │
│  │  (volume density)         (signed distance)              │    │
│  │                                                          │    │
│  │  SDF 정의:                                               │    │
│  │  - s(x) < 0: 표면 내부                                   │    │
│  │  - s(x) = 0: 표면 위 (zero level set)                    │    │
│  │  - s(x) > 0: 표면 외부                                   │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  Step 2: Tetrahedral Grid                                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                          │    │
│  │  3D 공간을 사면체(tetrahedra)로 분할                     │    │
│  │                                                          │    │
│  │       v1 ●───────● v2                                    │    │
│  │         /\      /                                        │    │
│  │        /  \    /                                         │    │
│  │       /    \  /                                          │    │
│  │   v0 ●──────●/ v3                                        │    │
│  │                                                          │    │
│  │  각 vertex에서 SDF 값 계산: s(v0), s(v1), s(v2), s(v3)  │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  Step 3: Marching Tetrahedra                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                          │    │
│  │  SDF 부호가 바뀌는 edge에서 표면 vertex 생성             │    │
│  │                                                          │    │
│  │  Case: v0, v1 < 0 (inside), v2, v3 > 0 (outside)        │    │
│  │                                                          │    │
│  │       v1 ●───────● v2                                    │    │
│  │         /\  ◆◆◆ /      ◆ = surface vertices             │    │
│  │        / ◆◆◆◆◆/                                         │    │
│  │       / ◆◆  ◆/                                          │    │
│  │   v0 ●──────●/ v3                                        │    │
│  │                                                          │    │
│  │  Edge intersection: p = v_a + t*(v_b - v_a)             │    │
│  │  where t = s(v_a) / (s(v_a) - s(v_b))                   │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  Step 4: Differentiable Rendering                                │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                          │    │
│  │  추출된 mesh를 differentiable하게 렌더링                 │    │
│  │  → RGB/Silhouette loss로 SDF 파라미터 최적화             │    │
│  │                                                          │    │
│  │  DMTet 장점:                                             │    │
│  │  - Marching Cubes보다 적은 아티팩트                      │    │
│  │  - End-to-end differentiable                             │    │
│  │  - Topology 변화 가능                                    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 DMTet 학습

```python
class DMTetStage(nn.Module):
    def __init__(self, nerf_model, grid_resolution=128):
        self.nerf = nerf_model  # Frozen or fine-tuned
        self.sdf_mlp = nn.Sequential(
            nn.Linear(nerf_feature_dim, 128),
            nn.ReLU(),
            nn.Linear(128, 1)  # SDF value
        )
        self.deform_mlp = nn.Sequential(
            nn.Linear(nerf_feature_dim, 128),
            nn.ReLU(),
            nn.Linear(128, 3)  # Vertex offset
        )

    def forward(self, image, camera):
        # 1. NeRF features 추출
        triplane = self.nerf.encode(image)

        # 2. Grid points에서 SDF 예측
        sdf = self.predict_sdf(triplane, grid_points)

        # 3. Marching tetrahedra로 mesh 추출
        vertices, faces = marching_tetrahedra(sdf, grid)

        # 4. Vertex deformation (optional refinement)
        offsets = self.deform_mlp(vertex_features)
        vertices = vertices + offsets

        return vertices, faces
```

### 5.4 DMTet 손실 함수

```python
L_dmtet = L_rgb + λ_mask * L_mask + λ_normal * L_normal + λ_lap * L_laplacian

# RGB 손실 (렌더링 일치)
L_rgb = ||render(mesh) - I_gt||_1

# Mask 손실 (실루엣)
L_mask = BCE(silhouette(mesh), M_gt)

# Normal 손실 (표면 부드러움)
L_normal = ||n_pred - n_gt||_2

# Laplacian 정규화 (메쉬 부드러움)
L_laplacian = ||Δv||_2  # 이웃 정점과의 차이 최소화
```

> **Implementation Status**: DMTet Stage 2는 현재 미구현 (config만 정의, `train.py`에서 skip). NeRF Stage 1만으로 이미지 렌더링 및 포즈 추정 가능. 상세: [Paper vs Implementation Audit](reports/260208_paper_vs_implementation_audit.md#5-dmtet-stage-2-analysis)

---

## 6. 학습 설정 (Paper)

### 6.1 하드웨어 요구사항

| Component | Specification |
|-----------|---------------|
| GPU | NVIDIA RTX 3090 (24GB) 이상 |
| RAM | 32GB 이상 |
| Storage | 100GB+ (합성 데이터) |

### 6.2 학습 파라미터 (Table A3)

| Stage | Parameter | Value |
|-------|-----------|-------|
| **Avatar** | Iterations | 400,000 |
| | Learning Rate | 1e-3 |
| | Image Size | 800×800 |
| | Frames | 800 |
| | Cameras | 8 |
| **Synthetic** | Scenes | 12,000 |
| | Views/Scene | 64 |
| | Image Size | 378×378 |
| **NeRF** | Epochs | 60 |
| | Batch Size | 4 |
| | Learning Rate | 1e-4 |
| **DMTet** | Epochs | 100 |
| | Grid Resolution | 128 |
| | Learning Rate | 1e-4 |

### 6.3 손실 가중치

```python
# Avatar Training
loss_weights = {
    'l1': 1.0,
    'ssim': 0.2,
    'lpips': 0.1,
}

# NeRF Stage
loss_weights = {
    'rgb': 1.0,
    'depth': 0.1,
    'mask': 0.5,
}

# DMTet Stage
loss_weights = {
    'rgb': 1.0,
    'mask': 1.0,
    'normal': 0.1,
    'laplacian': 0.01,
}
```

### 6.4 예상 학습 시간

| Stage | Time (RTX 3090) |
|-------|-----------------|
| Avatar Training | 24-48 hours |
| Synthetic Generation | 2-4 hours |
| NeRF Training | 8-12 hours |
| DMTet Training | 16-24 hours |
| **Total** | **~50-88 hours** |

---

## 7. 추론 (Inference)

```python
# 단안 이미지에서 3D 메쉬 추출
def inference(model, image, camera_params):
    """
    Args:
        image: [1, 3, H, W] - 입력 이미지
        camera_params: dict - 카메라 파라미터

    Returns:
        mesh: trimesh.Trimesh - 3D 메쉬
        novel_views: list - 새로운 시점 렌더링
    """
    with torch.no_grad():
        # 1. NeRF encoding
        triplane = model.encode(image)

        # 2. DMTet mesh extraction
        vertices, faces = model.extract_mesh(triplane)

        # 3. Novel view synthesis
        novel_views = []
        for view_camera in sample_cameras(num_views=8):
            rendered = model.render(triplane, view_camera)
            novel_views.append(rendered)

    return trimesh.Trimesh(vertices, faces), novel_views
```

---

## 참고문헌

1. MoReMouse: 3D Reconstruction of Laboratory Mice from Monocular RGB Images (2024)
2. 3D Gaussian Splatting for Real-Time Radiance Field Rendering (SIGGRAPH 2023)
3. Deep Marching Tetrahedra (NeurIPS 2021)
4. Instant Neural Graphics Primitives (SIGGRAPH 2022)
5. MAMMAL: Multi-Animal Articulated Model for 3D Animal reconstruction (2023)

---

## Related Documents

- [Refactoring Plan (2026-02-08)](refactoring/260208_refactoring_plan.md) — 3-phase code cleanup plan
- [Refactoring Report (2026-02-08)](refactoring/260208_refactoring_report.md) — Detailed results and metrics
- [Coordinate Alignment Analysis](reports/251213_moremouse_coordinate_alignment_analysis.md) — Y-up → Z-up transform rationale
- [Gaussian Avatar Paper Comparison](reports/251212_gaussian_avatar_paper_comparison.md)
- [GPU05 Pipeline Guide](guides/gpu05_pipeline_guide.md)

### Shared Utility Modules (added 2026-02-08)

| Module | Purpose |
|--------|---------|
| `src/utils/transforms.py` | Coordinate constants, Y-up→Z-up transforms, projection |
| `src/utils/geometry.py` | 22-keypoint definitions, extraction, skeleton drawing |
| `src/utils/avatar_visualization.py` | Debug visualization helpers |

---

*문서 생성일: 2024-12-09 | 최종 수정: 2026-02-08*
*MoReMouse Implementation v1.0*
